DATABASE_URL=postgresql+psycopg://dush:change-me@postgres:5432/dush
ADMIN_TOKEN=replace-with-a-long-random-token
# Prefix written to products.images; actual bytes are served by Nginx/object storage.
MEDIA_BASE_URL=/media
ENVIRONMENT=development
